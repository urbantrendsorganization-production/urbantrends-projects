# Base URL server components use to reach the backend API (compose service).
API_URL=http://localhost:8000
# Base URL the browser uses for client-side API calls (published host port).
NEXT_PUBLIC_API_URL=http://localhost:8000
